spring:
  application:
    name: bizflow-inventory-service

  datasource:
    url: jdbc:mysql://${DB_HOST:mysql-inventory}:3306/bizflow_inventory_db?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8&connectionCollation=utf8mb4_unicode_ci
    username: root
    password: ${DB_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  cache:
    type: none

  # Disable RabbitMQ and Redis auto-configuration
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisReactiveAutoConfiguration

  mvc:
    cors:
      allowed-origins: "*"
      allowed-methods: GET,POST,PUT,DELETE,OPTIONS
      allowed-headers: "*"
      allow-credentials: false
      max-age: 3600

server:
  port: 8084
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      force: true

app:
  catalog:
    base-url: ${CATALOG_SERVICE_URL:http://localhost:8083}

logging:
  level:
    root: WARN
    com.example.bizflow: WARN
