spring:
  application:
    name: bizflow-sales-service

  datasource:
    url: jdbc:mysql://${DB_HOST:mysql-sales}:3306/bizflow_sales_db?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8&connectionCollation=utf8mb4_unicode_ci
    username: root
    password: ${DB_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  cache:
    type: none

  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: 6379
      timeout: 6000

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASSWORD:guest}

  mvc:
    cors:
      allowed-origins: "*"
      allowed-methods: GET,POST,PUT,DELETE,OPTIONS
      allowed-headers: "*"
      allow-credentials: false
      max-age: 3600

server:
  port: 8081
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      force: true

app:
  jwt:
    secret: my-secret-key-for-jwt-token-generation-and-verification
    access-token-expiration: 3600000
    refresh-token-expiration: 86400000
  invoice:
    branch-prefix: TC
  catalog:
    base-url: ${CATALOG_SERVICE_URL:http://localhost:8083}
  inventory:
    base-url: ${INVENTORY_SERVICE_URL:http://localhost:8084}
  customer:
    base-url: ${CUSTOMER_SERVICE_URL:http://localhost:8085}
  auth:
    base-url: ${AUTH_SERVICE_URL:http://localhost:8086}
  promotion:
    base-url: ${PROMOTION_SERVICE_URL:http://localhost:8082}

logging:
  level:
    root: WARN
    com.example.bizflow: WARN
